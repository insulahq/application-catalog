# Insula platform — directives shared by every generated multi-host site.
#
# Included from inside each <VirtualHost> the platform writes into
# conf/insula/sites.d. Everything here is DOCUMENT-ROOT INDEPENDENT so a
# generated site is only ServerName / DocumentRoot plus this include.
#
# RemoteIP and the log format are NOT repeated: they are set once in the main
# server config and virtual hosts inherit them, so restating them here would
# create two copies that can drift.

# Filesystem policy for every site root in this container. Scoped to /var/www
# rather than to one docroot so a single block covers them all. Deliberately
# mirrors the stock <Directory> in httpd.conf — including `Indexes` — so a
# multi-host site and the catch-all behave the same way; a site that silently
# stopped listing directories when it moved onto a shared pod would be a
# regression nobody asked for.
<Directory "/var/www">
    # No directory listing. `Indexes` came from the stock httpd.conf and listed
    # any folder without an index file — a disclosure on a hosting platform,
    # not a feature.
    #
    # Symlink following stays ON, and not by choice. A symlink inside one site's
    # folder pointing at a neighbour's is served by Apache directly, and a
    # static runtime has no interpreter and therefore no open_basedir to fall
    # back on — so `-FollowSymLinks` is exactly the protection this file wants.
    # Apache will not allow it: with FollowSymLinks and SymLinksIfOwnerMatch
    # both off it refuses RewriteRule (AH00670), and the scheme-aware redirect
    # below is a RewriteRule, so every request returned 403. Measured against a
    # running pod, not reasoned about.
    #
    # SymLinksIfOwnerMatch would restore rewriting without restoring the
    # protection: every file on the volume has the same runtime uid, so an owner
    # check permits precisely the symlink that has to be refused.
    #
    # There is therefore no Apache configuration that both rewrites and refuses
    # symlinks. The residual: a tenant who authors a symlink between two of
    # their own sites can serve one from the other. That is not an escalation —
    # they already have access to both — but it is a real difference from the
    # nginx runtimes, which refuse symlinks with `disable_symlinks` and lose
    # nothing by it.
    Options -Indexes +FollowSymLinks
    AllowOverride All
    Require all granted

    # --- Scheme-aware directory redirect (platform reverse proxy) ---
    # TLS terminates at Traefik, so mod_dir's trailing-slash 301 (/work ->
    # /work/) would hardcode scheme http (the pod connection). Issue the
    # redirect ourselves so it honours the edge scheme from X-Forwarded-Proto.
    # (mod_headers cannot fix mod_dir's Location — it lives in headers_out,
    # which `Header always` does not touch.) In <Directory> context so
    # REQUEST_FILENAME resolves for the -d test.
    DirectorySlash Off
    RewriteEngine On
    RewriteCond %{HTTP:X-Forwarded-Proto} =https
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteCond %{REQUEST_URI} !/$
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI}/ [R=301,L,QSA]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteCond %{REQUEST_URI} !/$
    RewriteRule ^ %{REQUEST_URI}/ [R=301,L,QSA]
</Directory>
