replicaCount: 1

image:
  repository: ghcr.io/insulahq/application-catalog/static-nginx
  tag: "1.31.2"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false
  className: nginx
  domain: ""
  tls: true

resources:
  requests:
    cpu: 0.10
    memory: 64Mi
  limits:
    cpu: 0.10
    memory: 64Mi

persistence:
  enabled: true
  mountPath: "/var/www/html"
  size: 5Gi

env: {}
